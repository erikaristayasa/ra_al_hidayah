part of 'statics.dart';

enum AccountType {
  student,
  lecturer,
  parent,
}

enum BannerType {
  homepage,
  login,
}

enum GradeType {
  playgroup,
  tkA,
  tkB,
}

enum Gender {
  male,
  female,
}

enum PaymentStatus { process, accept, reject, draft }
